This repository contains a minimal single-page Jekyll site for the field-notes-microgrant project.

Preview locally

- Use a Jekyll-compatible environment to build and serve the site locally.

Enable GitHub Pages

- In the repository settings, enable GitHub Pages using the repository's default branch and the root (/) directory.

Notes

- The site is a single page (index.md) intended for GitHub Pages.
